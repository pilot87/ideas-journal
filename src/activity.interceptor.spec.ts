import { ActivityInterceptor } from './activity.interceptor';

describe('ActivityInterceptor', () => {
  it('should be defined', () => {
    expect(new ActivityInterceptor()).toBeDefined();
  });
});
