import {  action,  configure,  makeAutoObservable,  observable,  runInAction,} from 'mobx'import { auth } from './auth'export interface Idea {  author: string  ideaname: string  short_desc: string  tags: string[]  status: string}configure({ enforceActions: 'observed' })export class Ideas {  @observable ideas = new Array<Idea>(0)  @observable auto = false  @action update() {    auth.send      .get('/idea/listall')      .then((res: any) => {        runInAction(async () => {          this.ideas = res.data.list        })      })      .catch(() => console.log('update errors'))  }  @action setAuto(value: boolean) {    this.auto = value  }  constructor() {    makeAutoObservable(this, {}, { autoBind: true })  }}export const ideas = new Ideas()