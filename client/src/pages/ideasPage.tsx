import React, { useEffect } from 'react'import { Ideas, Idea } from '../features/ideas'import Jumbotron from 'react-bootstrap/Jumbotron'import Badge from 'react-bootstrap/Badge'import Container from 'react-bootstrap/Container'import Row from 'react-bootstrap/Row'import Col from 'react-bootstrap/Col'export const IdeasPage = (props: {  ideas: Idea[]  update: Ideas['update']  setAuto: Ideas['setAuto']  id: number}) => {  useEffect(() => {    props.setAuto(true)    return () => {      props.setAuto(false)    }  }, [])  return (    <Container fluid>      <p>{props.ideas.length.toString()}</p>      <p>{'id: ' + props.id.toString()}</p>      {props.ideas.map((idea) => (        <Row>          <Col className="p-3">            <Jumbotron>              <h1>{idea.ideaname}</h1>              <p>{idea.short_desc}</p>              <Row className="m-0 h-100">                {idea.tags.map((tag) => (                  <Col sm="auto">                    <Badge                      variant="secondary"                      className="w-auto h-75 mt-1 mb-1 align-middle pl-25 pr-25"                    >                      {tag}                    </Badge>                  </Col>                ))}              </Row>            </Jumbotron>          </Col>        </Row>      ))}    </Container>  )}