import React, { useEffect } from 'react'import { observer } from 'mobx-react-lite'import { useParams } from 'react-router-dom'import Badge from 'react-bootstrap/Badge'import Container from 'react-bootstrap/Container'import Row from 'react-bootstrap/Row'import Col from 'react-bootstrap/Col'import { viewIdea } from '../features/viewIdea'export const ViewIdeaPage = observer(() => {  const ideaname = useParams<{ idea: string }>().idea  useEffect(() => {    viewIdea.setName(ideaname)    viewIdea.update()    viewIdea.setAuto(true)    return () => {      viewIdea.setAuto(false)      viewIdea.clean()      viewIdea.setName('')    }  }, [])  const idea = viewIdea.idea  return (    <Container fluid>      <Row>        <Col>          <Badge variant="info">{idea.ideaname}</Badge>        </Col>      </Row>      <Row>        <Col>          <Badge variant="light">{idea.short_desc}</Badge>        </Col>      </Row>      <Row>        <Col>{idea.author}</Col>      </Row>      <Row>        <Col>{idea.describtion}</Col>      </Row>      <Row>        <Col>{idea.link}</Col>      </Row>      <Row>        {idea.tags.map((tag) => (          <Col sm="auto">            <Badge              variant="secondary"              className="w-auto h-75 mt-1 mb-1 align-middle pl-25 pr-25"            >              {tag}            </Badge>          </Col>        ))}      </Row>    </Container>  )})